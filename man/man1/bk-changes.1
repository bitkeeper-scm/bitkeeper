.so bk-macros
.TH "bk changes" 1 20%E% "\*(BC" "\*(UM"
.SH NAME
bk changes \- show changeset history
.SH SYNOPSIS
.Bc bk changes [
.OPTopt \-v num
.Bc ] [
.OPTreq \-r revs
 |\ - \c
.B ]
.SH OPTIONS
.if t .TP
.if n .TP 1i
.OPTreq \-r revs
Specifies the changesets to be listed, i.e., 1.100..
.tp
.B \-t
Only list changesets which are tagged.
.tp
.B \-v
Shows individual file change history as well as changeset history.
.tp
.OPTreq \-v num
As above, but indent per file 
.ARG num
spaces.
.SH EXAMPLES
Sample output:
.DS
ChangeSet@1.607, 2000-02-21 14:05:25-08:00, awc@host.com
  update citool to use the "bk unedit" interface. 

ChangeSet@1.606, 2000-02-21 13:35:21-08:00, awc@host.com
  This fix allows \*(BK to be installed in a non-standard directory.
  The install directory is computed from the $PATH variable and 
  the bk symlink.

ChangeSet@1.605, 2000-02-20 01:32:19-08:00, lm@host.com
  Fix a diagnostic in pull.
  An aborted attempt at key compression.
.DE
.LP
We use the following script to generate per user reports which skip
merge changesets, since there usually isn't interesting content there:
.DS
for p in $PROGRAMMERS
do	echo Generating summary for $p
	bk prs -hrbk-v1.2.9.. \e
	    -d'$if(:USER:='$p'){$unless(:MERGE:){:I:\en}}' ChangeSet |
	    bk changes - > $p.summary
done
for p in $PROGRAMMERS
do	echo Generating details for $p
	bk prs -hrbk-v1.2.9.. \e
	    -d'$if(:USER:='$p'){$unless(:MERGE:){:I:\en}}' ChangeSet |
	    bk changes -v - > $p.details
done
.DE
.SH "SEE ALSO"
.SA commit 1
.SA pending 1
.SA prs 1
.SA sccslog 1
.SA revtool 1
.SH CATEGORY
.B Repository
